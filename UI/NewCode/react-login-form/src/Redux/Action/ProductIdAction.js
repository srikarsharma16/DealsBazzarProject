import * as actiontype from './Action'

export const ACTION_SET_PRODUCT_ID={
    type:actiontype.SET_PRODUCT_ID,
    payload:{
        product:undefined
    }
}
export const LOGOUT={
    type:actiontype.LOGOUT,
    payload:{
        reset:undefined
    }
}